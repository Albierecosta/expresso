<%= render "shared/page_header", title: Recipient.model_name.human(count: 2) do %>
  <%= link_to t("actions.new"), new_admin_recipient_path, class: "btn-primary" %>
<% end %>

<% if @recipients.any? %>
  <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
    <table class="w-full">
      <thead class="bg-gray-50 text-left text-xs uppercase tracking-wider text-gray-500">
        <tr>
          <th class="px-4 py-3"><%= Recipient.human_attribute_name(:name) %></th>
          <th class="px-4 py-3"><%= Recipient.human_attribute_name(:client) %></th>
          <th class="px-4 py-3"><%= Recipient.human_attribute_name(:phone) %></th>
          <th class="px-4 py-3"><%= Address.human_attribute_name(:city) %></th>
        </tr>
      </thead>
      <tbody>
        <%= render partial: "row", collection: @recipients, as: :recipient %>
      </tbody>
    </table>
  </div>
  <div class="mt-4"><%== pagy_nav(@pagy) %></div>
<% else %>
  <%= render "shared/empty_state",
        title: "Nenhum destinatário cadastrado",
        action: link_to(t("actions.new"), new_admin_recipient_path, class: "btn-primary") %>
<% end %>
