<%# Wizard header used by every step of the public flow. %>
<div class="text-center mb-6">
  <h1 class="text-lg font-semibold text-brand-700"><%= t("app.name") %></h1>
  <p class="text-xs text-gray-500 mt-1"><%= t("public_flow.step", n: step) %></p>
  <h2 class="text-xl font-bold mt-4"><%= title %></h2>
</div>
